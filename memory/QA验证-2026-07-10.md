# QA 验证报告 — 2026-07-10

> 范围：**线上** https://game.pokeauto.online/（当前仍为**旧后端**；本仓新 `functions/` **未 deploy**）  
> 本地：`selfcheck` / `api-contract-selfcheck` / 版本号同步

## 结论（一句话）

**核心挂机体验可用且较平滑**（采集 / Tab / 商店 / Buff / 捕捉锁 / PVE UI）；**社交子系统在线上坏了（HTTP 500）**；排行榜核心 API 正常但榜面偏空。新后端需 wipe+deploy 后才能验证。

## 本地证据

| 检查 | 结果 |
|------|------|
| `node scripts/selfcheck.mjs` | OK |
| `node scripts/api-contract-selfcheck.mjs` | OK |
| 版本四处 `0.39.1` | 同步 |
| 新 `functions/` | 仅在 git（ahead），**未上线** |

## 线上 API 探针

| 端点 | 状态 |
|------|------|
| `/api/health` | 200 |
| `/api/server/buffs` | 200（6 key，level 0） |
| `/api/leaderboard/*` | 200（提交前 items 空；submit 后可见） |
| `/api/events/pull` | 200 |
| `/api/server/boss/bully` | 200（hp 1000） |
| `/api/auth/login`（错密） | 403（非 500） |
| `/api/save/meta`（坏 token） | 401 |
| `/api/score/submit` | 200 |
| `/api/friends/*` · `/api/social/*` | **500** |

## 玩法冒烟

| 路径 | 结果 | 体验 |
|------|------|------|
| 加载 + 跳过引导 | 通过 | 正常 |
| 采集 | 树果 1→2；按钮有冷却文案 | 平滑 |
| Tab 全开（13 个） | 均有面板内容 | 切换约 ~1s/圈，无卡死 |
| 未来商店 | 签到/月卡/礼包/兑币 UI 完整 | 平滑（空号无币属预期） |
| 捕捉 | 「研究精灵球基础后解锁」 | 预期锁 |
| 研究 | 按钮存在但资源不足 disabled | 预期 |
| 排行榜 UI | 可刷新；需昵称才能提交 | 榜数据偏空 |
| 全服 Buff 弹窗 | 可开可关 | 平滑 |
| 社交 Tab | UI 在，但控制台 500 | **不平滑**（空态+报错） |
| PVE / 道具 / 帮助 | UI 正常 | 帮助文案仍写 **v0.38**（文档漂移） |

## 控制台

仅见社交相关 500（messages / pvp-invites / friends / achievements）。未见前端白屏或模块加载失败。

## 风险与建议

1. **社交 500**：旧后端坏了；本仓新 `functions/` 已实现对应路由，需 **wipe D1 + deploy** 后复测。  
2. **未部署本地改动**：线上体验 ≠ 当前 git（含 `ui.fetch`、dailyTasks DOM、boss/leaderboard 外迁）。  
3. **帮助页版本**仍显示 v0.38，建议下轮改文案。  
4. 验证时向线上提交了测试分 `uid=qa-verify`（图鉴 1）；wipe 后会清掉，或可忽略。

## 是否「完好 / 平滑」

- **核心循环（采集→建造锁→研究锁→商店浏览）**：完好，体验平滑。  
- **全服系统（buff/boss/events/score）**：API 完好。  
- **社交**：功能**不完好**，体验有噪音。  
- **新后端**：尚未验证（未上线）。
