---
description: kittens 重构向开发 Loop — 长跑到队列空
alwaysApply: true
---

# kittens 开发 Loop（重构向 · 长跑）

## 触发

用户说「执行开发 loop」「继续」「长跑到队列空」，或 `/loop` 带「kittens 开发迭代」。

## 节奏（默认：马拉松）

- **同一会话连续完成所有可执行 `REF-xxx` / `FIX-xxx`**，直到：
  - 待做队列无可执行项，或
  - 遇到 blocked（见下）
- **不要**每条任务后停下来等用户说「继续」
- 每完成一条仍更新 `DEV_BACKLOG.md`；会话结束时统一写 `memory/*`
- 单条仍保持 min-diff；可在同会话内连续多条，但每条独立验收

## 每条任务流程

1. 读 `DEV_BACKLOG.md`、本规则与 `kittens-collab.mdc`
2. 选队首 `size:S/M` 且 deps 满足的任务（`size:L` 须先拆子项再做）
3. 只改该任务 `@window` 文件；单条任务内 app.js 尽量只动一个 SECTION
4. 重构：**行为不变**（除非任务写明）；FIX 可按任务写明做行为/卫生变更
5. 验证 → 勾选完成 → 立刻下一条

## 禁止

- 不自动 `deploy.ps1`、不自动 commit/push（除非用户明确说）
- 不删功能代码除非任务写明
- 不顺手改数值平衡
- 不 bump 版本号（重构/加固轮，除非任务写明）

## Blocked 时停止（仅这些）

- `tag:decision` 任务
- `size:L` 且用户未允许拆分/执行子项
- 跨窗口冲突无法在本会话安全合并
- 验证失败且一轮最小修复仍失败

## 完成汇报

队列空或 blocked 时输出：完成了哪些 ID、改了哪些文件、验证了什么、遗留风险、若有 blocked 写明原因。
